import { Env } from '../api/Env';
import { EnvRegistryItem } from '../api/EnvRegistry';
export declare const isRegisterRequestValid: (request: EnvRegistryItem) => boolean;
export declare const isEnvKeyValid: (envKey: string) => boolean;
export declare const isEnvValid: (env: Env) => boolean;
